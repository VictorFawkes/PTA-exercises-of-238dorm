#include<stdio.h>
int main()
{
	int n=0;
	scanf("%d",&n);
	
	double sum=0.0;
	for(int i=1;i<=n;i++)
	{
		if(i%2 == 1)
		sum += 1.0/(double)(2*i-1);
		else
		sum += -(1.0/(double)(2*i-1));
	}
	
	printf("sum = %.6f",sum);
	return 0;
} 
