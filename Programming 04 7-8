#include<stdio.h>
#include<math.h>
int main()
{
	int n=0;
	scanf("%d",&n);
	
	int a[n];
	for(int i=0;i<n;i++)
	scanf("%d",&a[i]);
	
	int odd=0,even=0;
	for(int j=0;j<n;j++)
	{
		if(a[j]%2 == 0)
		even += 1;
		else
		odd += 1;
	} 
	
	printf("%d %d",odd,even);
	
	return 0;
} 
